# VT-19 — DEV tasksheet

CODE-SCOPE: core/scripts/ core/doctrine/competencies/ .claude/skills/

The grammar, the router, and the two `applies:` lines the grammar change lets us fix.
`.claude/skills/` is declared for the reason D12 records: it is generated by `vteam update` from the
doctrine, so a doctrine ticket cannot avoid touching it, and `ALWAYS_LEGAL` covers `docs/` but not
its twin.

## Steps

- **T0** — do not widen the change beyond what the decision authorised. The owner picked (A); (B) and
  (C) stay closed even though the residual body-only matches are visible and tempting.
- **T1** — grammar first, in both the validator and the router, so a `type:` token cannot be written
  before it can be matched.
- **T2** — change only the tokens `type:` replaces, plus one that duplicates a label outright.
- **T3** — measure before claiming. The honest number is −3.8%, and one ticket got *bigger* for a
  reason that has nothing to do with routing; say both.
