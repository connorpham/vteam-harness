# <TICKET> — what I did, in plain words

## Scenario: <name the first thing you did or verified>
Given <the situation before>
When <the one action>
Then <what a person can see happened — a real, observable outcome>

## Scenario: <the boundary / the thing that must be refused>
Given <the situation>
When <the action that should be refused or handled differently>
Then <what the product does instead — the refusal, the message, the unchanged state>

## Appendix (for technical readers)
<selectors, queries, file paths, request/response — anything code. Never above.>
