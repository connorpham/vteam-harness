# Competencies — QA (index; the lane reads THIS, not the tree)

Generated by `competency_check.py --write-index`; the gate reds a stale row.
Load `always` rows at the step named in `loads`; load the others when a token
matches the ticket's labels, the touched paths, the stack profile, or a term in
the ticket/spec text. Every loaded file's **Reviewer lens** goes into the R1/R2 brief.

| Competency | Loads at | Applies | Use when… |
|---|---|---|---|
| `qa-accessibility-verification` | V2 | `label:ui, label:frontend, label:mobile, term:screen, term:form, term:dialog, term:modal, term:keyboard, term:contrast` | Use when verifying any user interface — web or mobile — and the ticket, spec or design says nothing about accessibility. Also when an automated scan came back clean, when a design hands over only the default state, and when 'accessible' has to become a pass or fail with a number behind it. |
| `qa-case-writing` | V2 | `always` | Use when writing a test case record or filling in its result — when a title names a topic instead of a behaviour, steps hide the typed value, EXPECTED says 'works correctly', or ACTUAL says 'failed'. |
| `qa-combinatorial-design` | V2 | `label:matrix, label:compatibility, term:combination, term:matrix, term:locale, term:platform, term:pairwise, term:flaky` | Use when a feature has several independent inputs and the honest combination count is too large to run — roles by plans by locales, payment method by currency by coupon, OS by browser by network by font scale. Also when deciding which tier a test belongs in, and when a suite is red often enough that people re-run it instead of reading it. |
| `qa-heuristics` | V2 | `always` | Use when the spec is silent and you need a defensible finding anyway, when choosing the exploratory case, or when a boundary needs a sharper edge — consistency oracles, coverage walks, data shapes, interruptions, tours. |
| `qa-hostile-inputs` | V2 | `always` | Use when choosing the concrete values for a boundary case — text, numbers, money, dates, files, emails, ids — and when a field has only ever been tested with the value the developer typed. |
| `qa-identity` | V1 | `always` | Use when a /qa session starts on any ticket, before deriving a single expected value — and whenever a verdict is about to be written from reading code, from the dev's claim, or from what 'obviously' should happen. |
| `qa-report-writing` | V5 | `always` | Use when writing the verification report or the evidence folder — when the reader is a product owner or developer who needs the verdict and the proof without reading code, and when 'PASS' rests on a folder nobody else can follow. |
| `qa-requirement-smells` | V1 | `always` | Use when reading a ticket or spec before verification — when criteria contain words like should, properly, fast, handle, show an error, the user, always — and when a 'done' claim rests on a sentence nobody could test as written. |
| `qa-security-probes` | V2 | `label:auth, label:security, label:payment, term:login, term:session, term:role, term:permission, term:password, term:upload` | Use when a ticket touches authentication, sessions, roles, money, personal data, uploads, or any server that trusts client input — the input an attacker sends on purpose, provable with the lane's own browser, API recorder and read-only database. |
| `qa-test-design` | V2 | `always` | Use when choosing the two-to-five cases for a verification — what deserves the budget, which boundary earns its place, when an exploratory or security slot is warranted — and when a case pack is all happy path. |
| `qa-user-mindset` | V2 | `label:ui, label:frontend, label:mobile, label:design, path:app/, path:components/, path:src/components/, term:screen, term:page, term:form, term:button, term:modal, term:dialog, term:ux` | Use when designing or running any UI verification — when every case in the pack could have been written by someone who has never watched a real person use software, and when a PASS came from typing perfect values in perfect order. |
