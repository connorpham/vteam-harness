# VT-27: Publish the vteam-vs-BMAD scorecard — whichever way it went
- status: In Review
- labels: benchmark, honesty, launch
- estimate: 0.5d
- type: Task

**30-second summary:** both arms ran on 2026-09-03/04 and were never judged. The held-out judge
was repaired (five environmental/probe defects, all applied before either arm was scored, all
recorded), re-calibrated to 91/91, and run. BMAD 100 %, vteam 92.3 %, three vteam "done" claims
contradicted. The result is published in docs/BENCHMARK.md with every caveat that belongs beside it.

## Why
README promised the scorecard "whichever way it goes". A framework whose pitch is proof over
promises cannot publish only the benchmarks it wins. Spec: docs/BENCHMARK.md (the stub this
replaces), harness PROTOCOL.md (owner's machine, held out). no UI.

## Acceptance criteria
- Given docs/BENCHMARK.md / When read / Then the headline states the loss in the first sentence, every number traces to docs/benchmark/2026-09-03/SCORECARD.md, and the six caveats (one sample, stop-state commit, no cost, judge repairs, native scores excluded, harness held out) are present.
- Given docs/benchmark/2026-09-03/JUDGE-REPAIRS.md / When read / Then each repair names what broke, the evidence, the fix, and that it was applied to both arms before scoring; the post-repair calibration reads 91/91.
- Given README.md / When read / Then the benchmark line states the result, not "in progress", and tests/e2e.mjs still verifies the README count claims.
- Given the three contradicted claims / When the ticket closes / Then their investigation exists as VT-28 (To Do) — not explained away here.

## Out of scope
Investigating why arm-a's own e2e passed AC-A07/D02/D07 (VT-28); re-running either arm; publishing the probes; any cost estimate.
