# VT-26: First-run page — audit-first README, the guide split out, a real demo, the dogfood post
- status: In Review
- labels: readme, launch, positioning
- estimate: 0.5d
- type: Task

**30-second summary:** 31 days, 6 stars, 0 outside issues, a 685-line README whose first code
block sat at line 43. The front page now leads with the ten-second `audit`, installs in two
commands, and links out; the long form moved verbatim to `docs/GUIDE.md`. Real transcripts
became an animated SVG; the code review became a post draft; the audience question went to
the decision queue (D17) instead of being assumed.

## Why
Stars come from distribution and time-to-first-value, not from code quality — the review of
2026-09-17 (proof: `evd/VT-24`, `evd/VT-25`) is strong and invisible. Spec: docs/launch/README.md
(the campaign's one home), spec §D17 in docs/pm/decisions.md. no UI.

## Acceptance criteria
- Given the README / When a stranger reads it / Then the first runnable command is `npx vteam-harness audit` within the first 15 lines and install is two commands
- Given tests/e2e.mjs and tests/conformance.mjs / When they run / Then the README count claims and the documented config block still verify (config block now read from docs/GUIDE.md)
- Given docs/assets/fence-demo.svg / When opened / Then every line is a real transcript captured on 2026-09-17 and the file is well-formed XML
- Given docs/pm/decisions.md / When read / Then D17 states the audience question two-sided with the README's default named as (a) and reversible

## Out of scope
Removing or hiding any lane, gate or provider; posting anything (drafts are the owner's to post); the benchmark scorecard (lands in docs/BENCHMARK.md from its own run).
