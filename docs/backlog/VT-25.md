# VT-25: Give the five untested tools a selftest or an e2e case
- status: In Review
- labels: framework, tests
- estimate: 0.5d
- type: Task

**30-second summary:** The 2026-09-17 line-by-line review found five scripts with no
selftest and no e2e coverage: `core/scripts/orca_team.sh`,
`profiles/nextjs-prisma/scripts/ui-evidence.mjs`, `ui_fidelity.mjs`, `auth.mjs`, and
`tools/prepublish-check.mjs`. The house rule — a check that has never been red does not
exist — applies to tools too.

## Why
`prepublish-check.mjs` guards every publish and has never been shown to refuse one;
`ui_fidelity.mjs` writes the fidelity.md the DEV gate reads. Spec: docs/team/ops.md
§selftests. no UI.

## Acceptance criteria
- Given `bash core/scripts/orca_team.sh --selftest` / When run offline with a fake `orca` on PATH / Then status/open-run/wait fallbacks and `trust` (create, idempotent, backup-once, mode kept, bad path refused, bad JSON refused) are proven green and red
- Given `node profiles/nextjs-prisma/scripts/{auth,ui_fidelity,ui-evidence}.mjs --selftest` / When playwright is NOT installed / Then the pure logic (sign-in flow on a fake context, colour/px/font comparison + intent grammar, shots.json validation + headed policy) is proven without a browser
- Given `node tools/prepublish-check.mjs --selftest` / When run against a fixture repo with a stubbed npm / Then each of the five refusals is shown to fire and the clean path passes
- Given tests/e2e.mjs / When it runs / Then it invokes those selftests, and README's counts follow
- Given each of the five scripts / When run with `--selftest` (or exercised by tests/e2e.mjs) / Then a green path and at least one forced red are proven
- Given `prepublish-check.mjs` / When the tree is dirty or the version exists / Then the refusal is proven offline with a fixture

## Out of scope
Behaviour changes to the tools themselves.
