# Spec shards — index

Shards are VERBATIM extracts of the source documents (verbatim_gate.py proves it
byte-for-byte, by requirement code). Interpretation lives only in each shard's
BA NOTES section. Decided requirement deltas live in changes.md, never in shards.

| Feature | File | Scope |
|---|---|---|
