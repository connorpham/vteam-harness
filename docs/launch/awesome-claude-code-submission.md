# DRAFT — awesome-claude-code submission

> **DRAFT — the owner posts this, not an agent.** Submission is ONLY via the
> automated GitHub issue form ("Recommend a Resource") at
> https://github.com/hesreallyhim/awesome-claude-code/issues/new/choose — human PRs
> are rejected. One resource per issue.
>
> **Eligibility check before posting (the template enforces one of these):**
> (a) ≥14 days of active development since the first commit on the default branch,
> OR (b) ≥100 stars. Verify whichever holds on the day of submission; if neither
> does yet, wait.

Fields below map 1:1 to the issue form (`recommend-resource.yml`, fetched
2026-08-18).

---

**Title (issue title):**

```
[Resource]: vteam
```

**Display Name:**

```
vteam
```

**Category** (dropdown — single choice):

```
Agent Orchestration
```

*(Rationale: the list has no verification/QA category. vteam is a PM/BA/SA/DEV/QA
orchestration harness, so Agent Orchestration is the honest fit; "Security" would
oversell the secret-scan angle.)*

**Link:**

```
https://github.com/connorpham/vteam-harness
```

**Author Name:**

```
connorpham
```

**Author Link:**

```
https://github.com/connorpham
```

**Description** (1–3 sentences, descriptive not promotional, 10–500 chars):

```
A virtual AI team (PM/BA/DEV/QA workflows) for Claude Code and other agent tools,
built around proof-of-done: 15 machine gates that fail red when an agent claims
work it can't prove — review dossiers enforced at git push, evidence files
validated by scripts, verdicts pinned to commits. Every checking gate ships a
--selftest mutation proof, and `npx vteam-harness audit` grades any repo's
agent-accountability 0-100 without installing anything.
```

*(388 characters — within the 10–500 limit. Tone check: describes what it does,
does not address the reader.)*

**Checklist** — check the five required boxes truthfully (visit the repo first;
links working; Claude Code-specific; CONTRIBUTING.md read). **Leave the final
trap checkbox UNCHECKED** — the template plants a "do not check this box" item to
catch people who don't read.
