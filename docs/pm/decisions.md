# Decision queue — everything that needs the owner, in one place

Statuses: `🔴 OPEN` · `🟡 PROVISIONAL (machine) <date> — pending acceptance` ·
`✅ DECIDED <date>`. Rows are NEVER deleted. Deadlines are real dates
(YYYY-MM-DD) — a deadline written as words is invisible to every reminder
machine (schedule_check warns on them).

## 1. Open questions

| # | Question (searched-where · two-sided proposal · reversal cost) | Blocks | Status | Due |
|---|---|---|---|---|
| D1 | Keep the stored .checkpoint file (PR #35) or rework resume as a derived reader? Stored = convenient but a second source of truth no gate keeps honest (ops-247 §1); reader = derives from claim/branch/evidence/ledger, cannot lie. Reversal cost of reader: none (it stores nothing). | PR #36 | ✅ DECIDED 2026-08-24 — reader; checkpoint store deleted, doctrine amended per supersession law | 2026-08-24 |
| D3 | Owner escalation: the 2026-08-24 review + rework rounds (PR #36, #37, self-install) ran on `frontier` at the owner's direct instruction ("tiến hành hoàn thiện") — recorded here so perf_report's frontier flag has its approval trail. | — | ✅ DECIDED 2026-08-24 — approved by owner | 2026-08-24 |
| D2 | Should the vteam repo adopt vteam itself? Cost: rendered docs + gate CI in-repo; benefit: self-audit 62/C → 91/A, every release now ledgered and evidenced. Reversal: delete config + .vteam + docs/team (uninstall path documented in Known limits). | VT-1 | ✅ DECIDED 2026-08-24 — adopted; minimal own fence kept (stated in evd/VT-1/manifest.md) | 2026-08-24 |
| D4 | Field-study breadth: read the core of each domain (766 files) or all 2,622? Core = reviewable in one sitting, risks missing the tail where the rare cases live; all = complete map, large review burden. Reversal: none (reading only). | VT-13 | ✅ DECIDED 2026-09-10 — owner chose all 2,622 | 2026-09-10 |
| D5 | Mobile scope: both natives (android+ios+kotlin+swift-ui, 592 files), cross-platform only (202), or one of each (284)? Both natives = deepest on the platform differences that actually break, skips cross-platform leaks. Reversal: none. | VT-13 | ✅ DECIDED 2026-09-10 — owner chose both natives | 2026-09-10 |
| D6 | Depth strategy: map only, follow the ~24k curated links, or map + small code experiments? Links = slow and network-bound; experiments = catches what I only think I understood. Reversal: none. | VT-13 | ✅ DECIDED 2026-09-10 — owner chose map + code experiments (34/34 JS assertions and all-pairs coverage verified by execution) | 2026-09-10 |
| D7 | "Upgrade the project" = vteam (install the new competencies) or upstream developer-roadmap (file issues for the curriculum gaps) or both? Reversal for vteam: revert the PR. | VT-13 | ✅ DECIDED 2026-09-10 — owner chose vteam | 2026-09-10 |
| D8 | `qa-accessibility-verification`: `always` or conditionally routed? `always` guarantees a11y is never skipped but adds ~1,100 words to every QA session on a lane already at 6,367; routed costs nothing on a UI-less ticket but relies on `label:ui`/`term:form` matching. I recommended `always` in the assessment, then reversed after measuring the lane was near saturation and a11y is meaningless without a UI. Reversal cost: one token in frontmatter. | VT-13 | ✅ DECIDED 2026-09-11 — **keep `qa-accessibility-verification` routed**, decided on a measurement the original assessment lacked. `route_check` across the field trial's nine tickets: it loads on **9/9 with zero misses**, via `label:ui`+`term:screen`+`term:contrast`. So routed buys identical coverage to `always` and still costs nothing on a ticket with no UI, which was the whole argument. Ruled together with D9, on the same evidence. One real hole closed while deciding: `a11y` is the project's 4th most-used label and **no competency routed on it** — `label:a11y` and `label:accessibility` added to `qa-accessibility-verification` and `dev-frontend-craft`. | 2026-09-17 |
| D9 | `qa-user-mindset` moved from `always` to routed on the same reasoning (its description says "any UI verification"). Same reversal cost: one token. | VT-14 | ✅ DECIDED 2026-09-11 — **keep `qa-user-mindset` routed**, decided on a measurement the original assessment lacked. `route_check` across the field trial's nine tickets: it loads on **9/9 with zero misses**, via those plus `path:components/`+`term:button`. So routed buys identical coverage to `always` and still costs nothing on a ticket with no UI, which was the whole argument. Ruled together with D8, on the same evidence. One real hole closed while deciding: `a11y` is the project's 4th most-used label and **no competency routed on it** — `label:a11y` and `label:accessibility` added to `qa-accessibility-verification` and `dev-frontend-craft`. | 2026-09-17 |
| D10 | Lane-step vocabulary for `design`, `devops`, `sa`, `specialists`: what are their numbered steps? `/dev` has T0–T6, `/qa` V0–V7, `/ba` B0–B5, `/pm` P0–P4; these four have none, so `loads` has no honest value. `competency_check` does NOT validate the value, so an invented step passes the gate while guaranteeing the file is never loaded — a green that lies. Options: (a) name steps per lane, (b) add a workflow for each first, (c) leave the four roles without competencies. Reversal: cheap either way (frontmatter only). | the next competency ticket | ✅ DECIDED 2026-09-11 — **option (c): no competencies for the four, because none is a lane with its own step vocabulary.** Read, not assumed: **SA** is `## The SA lane — ADRs` *inside* `pm.md`, reached from P2 and from `/pm sa <topic>`; **DESIGN** is `## The DESIGN lane` in the same file, dispatched when a UI ticket has no design oracle. Both execute as /pm, so their craft belongs in a `role: pm` competency loading at **P2** — the vocabulary exists already. **devops** is a raci role (CI + gates), not a pipeline. **specialists** is the `.claude/agents/*` family: subagents a lane spawns, so their craft travels in the spawning lane's competencies — which is how `qa-automation-specialist` already worked on TB-8. The live half of this question is closed separately: since VT-20 `competency_check` validates `loads` against the role's workflow (proven with `loads: P9`), so an invented step reds instead of silently never loading. **Bonus:** `/pm`'s missing P3 is not a hole — `P-DECIDE`, `The SA lane` and `The DESIGN lane` occupy that slot, named rather than numbered. Noted in `pm.md` so nobody re-files it. | 2026-09-24 |
| D11 | `.githooks/pre-push` diverges 117 lines from the packaged version; `vteam update` parks `pre-push.new` on every run. Options: (a) keep this repo's own fence and delete the parked file, (b) merge the framework version in. This repo deliberately customises its fence ("the rules this repo ships to others apply to itself"), which argues for (a). Currently held in `git stash@{0}` so the publish gate could pass. Reversal: trivial. | publish hygiene  **Analysed 2026-09-10 (not applied — this one changes how you push, so it is yours).** The two files are not variants of one idea: the local 36-line hook is a minimal fence, the packaged 121-line one is a strict **superset** on every axis — same single secret-pattern grep, but `ALLOW_PUSH_MAIN` handled more thoroughly, plus `ALLOW_PUSH_NOREVIEW`, a hatch log (12 references vs 2), a config-driven `git.branch_pattern`, a review-dossier check, and `protected_branch` read from config instead of hardcoded `main`. Every edge the local hook handles is handled there too: ZERO-sha branch deletion, `rev-parse --verify` fallback, and `hash-object -t tree /dev/null` as the diff base on a repo with no `origin/<protected>` yet. `bash -n` clean. **Recommendation: adopt the packaged hook.** The local one's own header says its purpose is "the rules this repo ships to others apply to itself" — and the packaged hook *is* those rules, so keeping a weaker fork contradicts its stated reason to exist. **What it will cost you, immediately:** `core.hooksPath` is already `.githooks`, so the hook is live, and every VT-15..VT-19 commit is on `main`. The packaged version blocks a direct push to the protected branch without `ALLOW_PUSH_MAIN=1`, and blocks a push touching code paths without a review dossier. So adopting it means either branching from here on, or declaring the bypass each time and leaving the hatch-log line. That is a workflow change, which is why it was analysed and left for you rather than applied. | ✅ DECIDED 2026-09-11 — adopted the packaged hook. Both enforcement paths tested live with `git push --dry-run`: a direct push to `main` is refused with the PR instruction, and a branch carrying product code that does not match `git.branch_pattern` (`^(feat|fix)/VT-[0-9]+-`) is refused with the rename instruction. The local 36-line fork is kept at /tmp only for this session; the packaged hook is now `.githooks/pre-push`. **Consequence from now on:** every push to this repo needs a ticket-keyed branch and a PR, or a declared `ALLOW_PUSH_MAIN=1` / `ALLOW_PUSH_NOREVIEW=1` that writes a hatch-log line. All current work sits on local `main` and will need a branch to reach the remote. This also un-disables `review_check`, which `/dev` names four times and which nothing was running | 2026-09-17 |

| D12 | **`graph_check`'s ALWAYS_LEGAL covers one doctrine deploy target and not its twin.** The list is `docs/ evd/ .vteam/ .githooks/ .github/ vteam.config.yaml .gitattributes .gitignore`. `docs/` silently includes `docs/team/`, the doctrine mirror `vteam update` generates; `.claude/skills/` is the other mirror of the same generated source and is **not** on the list. So every doctrine ticket trips MAST 2.3 unless it remembers to name `.claude/skills/` in its CODE-SCOPE — which VT-15 did not, and the gate correctly reded it. Options: (A) leave as is — the gate asks for a deliberate widening and that is arguably the point; (B) add `.claude/skills/` to ALWAYS_LEGAL, making the two mirrors symmetric, at the cost that a commit could silently rewrite a deployed skill without scope review; (C) teach graph_check that a path is always-legal when it is a KNOWN GENERATED target declared by the adapter, so the rule follows the generator rather than a hand-kept list. Reversal: A→B is one line; C is real work. Recorded from VT-15, where the gate caught the author. | none — loosening scope enforcement is the owner's call, and (C) changes a gate's contract | ✅ DECIDED 2026-09-10 — owner chose **option (A): leave it as is**. The gate asking for a deliberate widening of CODE-SCOPE is the point, not a bug: it caught this author on VT-15's first commit, and the fix was one honest line in a tasksheet. Adding `.claude/skills/` to ALWAYS_LEGAL would let a commit rewrite a deployed skill with no scope review, which is a worse trade than remembering to declare it. Consequence to live with: every doctrine ticket must name `.claude/skills/` in its CODE-SCOPE — VT-19 does. | 2026-09-24 |

| D13 | **How should `applies:` stop over-routing?** Measured (VT-16, `evd/VT-16/dev/routing-cost.md`): the field trial's worst DEV ticket opens **15 competency files ≈ 17,270 tokens**, and 5 of the 9 dev ticket/lane pairs load at least one competency pulled in by a word appearing ONLY in the ticket's prose — TB-6, a CSS focus-ring ticket, loads `dev-mobile-craft` on the word "permission" and `dev-query-performance` on "pagination"; TB-3 loads `dev-async-work` on "worker", which there means "Orca worker", an agent. **Already tried and rejected with numbers:** matching `term:` only against title+labels+summary cuts 17 percent overall and 38-42 percent on the worst tickets, but drops 3 false matches and **12 true ones** — TB-7/TB-9 lose `dev-debugging` although both are `type: Bug`, TB-3/TB-4 lose `dev-api-design` although both add a real endpoint, three tickets lose `qa-security-probes` which `/qa` explicitly wants on auth work. So the fault is not the match scope, it is that several tokens are single common English words. Options: (A) extend the grammar with a `type:` token, so `dev-debugging` routes on the ticket's own `type: Bug` field — the structural truth the ticket already states, which no current token kind can express; (B) require a `term:` to land in a structural field (title / labels / summary / criteria) or appear twice; (C) cap the routed set per lane and rank by match strength; (D) leave it and use `route_check --weak` as a review habit. Reversal: A and B are grammar changes every competency depends on and every `applies:` line may need revisiting; C changes what a lane loads without changing any declaration; D costs nothing. VT-16 deliberately changed no `applies:` line pending this. | none — a grammar change is a contract every competency reads, and picking a cap is a judgement about what a lane may forget | ✅ DECIDED 2026-09-10 — owner chose **option (A)**: add the `type:` token first, then re-measure. Shipped as VT-19 and measured: 84 → 81 competency files, −3.8% (−6.0% excluding TB-9, which grew for unrelated reasons). Routing on the two Bug tickets is now structural rather than prose-triggered. **Residual, and it needs a second decision:** 5 of 9 dev pairs still carry a body-only match — TB-9, a focus-outline ticket, loads `dev-async-work` because the word "queue" appears once in its prose. Options (B) and (C) remain the candidates for that; they were deliberately not taken here. | 2026-09-24 |

| D14 | **A file the repo has deliberately forked gets a `.new` parked on it every single `vteam update` — forever.** The manifest guard is right to never overwrite a user edit, but "review the pair and delete the `.new`" is written as a one-time action and is actually a recurring chore: `.vteam/profiles/nextjs-prisma/gates.yaml` in the field repo carries monorepo routing the stock profile will never have (KI-001), so a `.new` reappeared within an hour of the last one being deleted under A3 — and `git add -A` swept it into a commit before it was noticed. `.githooks/pre-push` in this repo is the same shape (D11). Proposal: let the repo **declare ownership** of a path — a `forked:` list in `.vteam/manifest.json`, or a marker line in the file — so `vteam update` stops parking `.new` for it and instead reports once, at the end: "3 files are owned locally; upstream changed 1 of them since your fork". That keeps the signal (upstream moved) and drops the chore (a file to delete every time). Alternatives: (B) leave it and treat the recurring `.new` as the reminder it accidentally is; (C) teach `vteam doctor` to list stale `.new` files so at least nothing silently accumulates. Reversal: A and C are additive; B is free. | none — it changes what `vteam update` promises about user edits | ✅ DECIDED 2026-09-11 — option (A), shipped in VT-21. `.vteam/manifest.json` takes an `owned: [paths]` list; a declared path is never clobbered AND never parked, and `vteam update` reports once that upstream moved under it. Proven on the field repo's forked `gates.yaml`: the `.new` stopped reappearing and the signal survived. **Deliberately NOT declared owned: `.githooks/pre-push`** — D11 is still open, and silencing its `.new` would hide a pending decision behind a convenience | 2026-09-24 |

| D15 | **`/plan` and `/pm` share the `P` step prefix, with a different meaning at every number.** `/plan` is P0 SCOPE · P1 THE BRIEF · P2 THE PRD · P3 ARCHITECTURE SPINE · P4 WIRE THE GATES; `/pm` is P0 PREFLIGHT · P1 PICK THE WORK · P2 DISPATCH · **(no P3)** · P4 YOUR DESK. So `loads: P1` means two different things depending on a lane the token does not name, and only the competency's separate `role:` field disambiguates it. Nothing has broken yet — one competency uses a `P` step (`pm-prioritisation`, `loads: P1`, `role: pm`) — but the collision is a trap for the next one, and `competency_check` resolves `loads` through `role`, so a mistake there is silent. Related: `/pm`'s numbering has a hole at P3 with no trace in git of a removed step. Options: (A) renumber `/plan` to its own prefix (`N` for planning, say) — touches every doc citing a plan step; (B) renumber `/pm` and fill the P3 hole — touches fewer docs, but `/pm` is the lane most often cited in prose; (C) leave it and make `competency_check` require `loads` to be written lane-qualified (`pm:P1`) — a grammar change, but it removes the ambiguity at the point where it matters; (D) leave it entirely, since `role:` already disambiguates and the checker now validates `loads` against that role's workflow (VT-20). Reversal: A and B are mechanical renames; C is a grammar change every competency reads; D is free. | none — a step vocabulary is cited across workflows, docs and every competency's frontmatter | ✅ DECIDED 2026-09-11 — option (D), leave the `P` prefix collision as it is. `role:` already disambiguates, and VT-20 closed the trap that made it dangerous: `competency_check` now resolves `loads` against that role's workflow and reds a value naming no step there — proven with `loads: P9`. Renaming a step vocabulary would touch every workflow, doc and frontmatter that cites it, to fix an ambiguity no longer reachable. `/pm`'s missing P3 stays a numbering hole and is recorded here rather than filled, because inventing a step to make the sequence tidy is worse than a gap with a reason | 2026-09-25 |

| D16 | **`log_check`'s foreign-key test is a denylist of acronyms, and a denylist has no closing condition.** A ledger row citing a decision the queue does not hold is red — unless the row also contains something shaped like a foreign ticket key, in which case it is a warning, because the row may be about another project. Round 1 of review broke that with `UTF-8`; it is now letters-only minus an exclusion list (UTF, SHA, MD, RFC, ISO, PR, CVE, HTTP, WCAG, ADR, TC), and round 2 broke it again with `AES-256`, `IEEE-754`, `SC-1.4.11`, `TLS-1`. Each fix is one incident behind. Options: (A) invert it — match against project keys the repo declares it works with (a new `tracker.foreign_keys:` list), so an unknown acronym reds instead of excusing; (B) drop the foreign-key concept and make every unresolvable citation a warning, losing the distinction but ending the arms race; (C) keep extending the denylist. Reversal: A needs a config key and a default; B is a deletion; C is free and endless. | none — (A) adds a config surface every repo inherits | 🔴 OPEN | 2026-09-25 |

## 2. Owner-only actions

| # | Action | Why machine-exempt | Status | Due |
|---|---|---|---|---|
| A3 | **Publish the next version to npm.** ~~0.18.0~~ — **0.18.0 went out on 2026-09-10 08:17 UTC**, so the original blocker is spent; `npm view vteam-harness versions` lists it. What is unpublished now is everything since: VT-15 through VT-22, which is a **0.19.0**. Two things stand between here and the registry and neither is mine: this machine's npm session is **not logged in** (`npm whoami` → 401 Unauthorized), and publishing needs the 2FA OTP. | npm 2FA is owner-held; an OTP cannot be delegated | 🔴 OPEN — **everything else is done.** 0.19.0 is merged to main (PR #67, CI green), `npm run prepublishOnly` passes all five refusals (clean tree · HEAD == origin/main 6696f96 · 0.19.0 free on the registry · npm test green · no .pyc in the 184-file tarball), and `npm publish` reaches the registry and stops at **`EOTP`** — the 2FA one-time password, which only the owner has. The earlier note that this machine was not logged in was wrong: `whoami` 401'd once, but the publish authenticated. One command remains: `npm publish --otp=XXXXXX` from the repo root | 2026-09-17 |

## 3. ADRs pending

| ADR | Decision | Status |
|---|---|---|
